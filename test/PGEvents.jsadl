aModule PGEvents:

    exports:
        aClass: 'EventEmitter'

    global:
        aClass: 'EventEmitter'
        name:   'PhoneGap.EventEmitter'

anInterface EventEmitter:

    methods:

        addListener: (event, listener) ->
            param  'string'
            param  'function'
            return ''

        on: (event, listener) ->
            param  'string'
            param  'function'
            return ''

        once: (event, listener) ->
            param  'string'
            param  'function'
            return ''

        removeListener: (event, listener) ->
            param  'string'
            param  'function'
            return ''

        removeAllListeners: (event) ->
            param  'string'
            return ''

        setMaxListeners: (n) ->
            param  'number'
            return ''

        listeners: (event) ->
            param  'string'
            return 'function[]'

        emit: (event) ->
            param  'string'
            param  '...'
            return ''

    events:
        newListener: (event, listener) ->
            param  'string'
            param  'function'
            return 'void'
